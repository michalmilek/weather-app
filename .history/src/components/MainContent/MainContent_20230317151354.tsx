import React from "react";
import Box from "@mui/material/Box";
import Image1 from "./Image1.jpg";

const MainContent = () => {
  return <Box sx={{ background: `url(${Image1})` }}>MainContent</Box>;
};

export default MainContent;
