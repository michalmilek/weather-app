import AppBar from "@mui/material/AppBar";

import React from "react";

const Navbar = () => {
  return (
    <AppBar
      position="relative"
      color="primary"
      sx={{
        display: "flex",
      }}>
      Navbar
    </AppBar>
  );
};

export default Navbar;
